# Vulnerabilities in Node.js base image's bundled npm dependencies.
# These are upstream issues we cannot fix — waiting on node:24-slim update.
# Review periodically and remove once patched.

# minimatch DoS via crafted glob patterns (npm bundled)
CVE-2026-26996
CVE-2026-27903
CVE-2026-27904

# tar arbitrary file read/write via hardlink (npm bundled)
CVE-2026-26960
GHSA-qffp-2rhf-9h96

# tar hardlink path traversal via drive-relative linkpath (npm bundled)
CVE-2026-29786

# tar node-tar <7.5.11 vulnerability (npm bundled)
CVE-2026-31802
